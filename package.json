{
  "name": "cym-client-preview",
  "version": "2.0.0",
  "private": true,
  "description": "Craig Young Music — client preview & approvals platform (admin + client logins, uploads, persistent store)",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "multer": "^1.4.5-lts.1"
  }
}
